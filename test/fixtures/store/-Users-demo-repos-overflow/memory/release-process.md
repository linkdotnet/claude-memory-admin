---
name: release-process
description: Tag, verify, publish - the order the workflow expects
metadata:
  type: feedback
---

Tag, verify, publish - the order the workflow expects. This note exists so the
overflow fixture has real memory files behind its index entries rather than a
wall of dangling pointers.
