---
name: editor-setup
description: Preferred formatter and the settings that fight it
metadata:
  type: user
---

Preferred formatter and the settings that fight it. This note exists so the
overflow fixture has real memory files behind its index entries rather than a
wall of dangling pointers.
