---
name: api-conventions
description: Error envelope and pagination shape used by every endpoint
metadata:
  type: reference
---

Error envelope and pagination shape used by every endpoint. This note exists
so the overflow fixture has real memory files behind its index entries rather
than a wall of dangling pointers.
