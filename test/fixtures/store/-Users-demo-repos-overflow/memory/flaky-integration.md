---
name: flaky-integration
description: The integration suite fails on a cold container, retry once
metadata:
  type: project
---

The integration suite fails on a cold container, retry once. This note exists
so the overflow fixture has real memory files behind its index entries rather
than a wall of dangling pointers.
