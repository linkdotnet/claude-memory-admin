---
name: test-layout
description: Where tests live and which ones are slow
metadata:
  type: project
---

Where tests live and which ones are slow. This note exists so the overflow
fixture has real memory files behind its index entries rather than a wall of
dangling pointers.
