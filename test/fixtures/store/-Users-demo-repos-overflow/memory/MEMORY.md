---
name: MEMORY
description: Index for the overflow demo project
---
<!-- Maintainer note: this index has drifted well past the load limit. -->
# Project

- [Build commands](build-commands.md) — How the solution is built, and the flags that matter

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

- [Test layout](test-layout.md) — Where tests live and which ones are slow
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

- [Api conventions](api-conventions.md) — Error envelope and pagination shape used by every endpoint
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
- [Db migrations](db-migrations.md) — Migrations run on startup in dev and never in prod

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

- [Logging buffering](logging-buffering.md) — Which middlewares need request buffering and why
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.
- [Release process](release-process.md) — Tag, verify, publish - the order the workflow expects
  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
- [Cache invalidation](cache-invalidation.md) — Permission reads are cached per request, cleared on write

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
the one that survived review, so prefer it over what the older commits suggest.

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
- [Editor setup](editor-setup.md) — Preferred formatter and the settings that fight it

  Worth knowing before you touch this: the behaviour is load-bearing and the
  test that covers it is not obvious from the name.

Historical note. This was changed twice and reverted once; the current shape is
- [Flaky integration](flaky-integration.md) — The integration suite fails on a cold container, retry once
