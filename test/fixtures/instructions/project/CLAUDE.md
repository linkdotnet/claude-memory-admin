# Alpha

See @docs/conventions.md for the house style.

To mention a path without importing it, wrap it in backticks: `@README` stays
literal. So does this, inside a fence:

```
@docs/never-imported.md
```

Contact @team@example.com about anything else.

- git workflow @docs/missing-file.md
